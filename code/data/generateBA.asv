N=500;
m=3;
m0=4;

A=BAGraph(N,m,m0);
G = graph(A);
edges=table2array(G.Edges);

m=numedges(G);
outFile = fopen('baGraph.txt','w');

for i =1:m
    fprintf(outFile,'%d\t%d\n', greedyRst(i));
end
fclose(outFile);